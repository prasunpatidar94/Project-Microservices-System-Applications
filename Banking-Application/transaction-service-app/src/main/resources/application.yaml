server:
  port: 9002
spring:
  application:
    name: transaction-service-app
  datasource:
    url: jdbc:h2:mem:transaction
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true